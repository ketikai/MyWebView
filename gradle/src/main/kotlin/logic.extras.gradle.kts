import logic.LogicMetadata

ext {
    set("NAME", LogicMetadata.NAME)
    set("NAMESPACE", LogicMetadata.NAMESPACE)
    set("COMPILE_SDK", LogicMetadata.COMPILE_SDK)

    set("APPLICATION_ID", LogicMetadata.APPLICATION_ID)
    set("COMMIT_CODE", LogicMetadata.COMMIT_CODE)
    set("VERSION_CODE", LogicMetadata.VERSION_CODE)
    set("VERSION_NAME", LogicMetadata.VERSION_NAME)
    set("MIN_SDK", LogicMetadata.MIN_SDK)
    set("TARGET_SDK", LogicMetadata.TARGET_SDK)
}
